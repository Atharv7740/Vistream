// import { Skeleton } from "@/components/atom/Skeleton"

export default function CategoriesFallback() {
    return (
        <h1 className="w-full h-[20vh] text-center text-5xl bg-indigo-400">Hello</h1>
        // <div className="flex flex-col gap-10">
        //     <ul className="h-[40vh] overflow-y-scroll flex flex-col gap-4 scrollbar-hidden  border-8 border-black">
        //     {new Array(12).fill(0).map((e, index) => (
        //         <Skeleton key={index} className="min-w-[200px] min-h-[300px] bg-black " />
        //     ))}
        // </ul>
        // <ul className="h-[40vh] overflow-y-scroll flex flex-col gap-4 scrollbar-hidden border-8 border-black">
        //     {new Array(12).fill(0).map((e, index) => (
        //         <Skeleton key={index} className="min-w-[200px] min-h-[300px] bg-black " />
        //     ))}
        // </ul>
        // </div>
        
        
    )
}